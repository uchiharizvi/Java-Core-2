package DS.trees.binarytree.problems.topview;

import java.util.LinkedList;
import java.util.Queue;

public class TopViewTree {
    Node root;

    public TopViewTree() {
        root = null;
    }
    private void topView(Node root){
        Queue<TopViewQueue> queue = new LinkedList<>();
        /*Map<Integer, Node> topViewMap =n*/
    }
}
